import { Alert } from "antd";

const WarningBlame = () => {
  return (
    <Alert
      message="Take your own risk. Don’t blame if any issue happens in intranet."
      type="warning"
      showIcon
    />
  );
};

export default WarningBlame;
